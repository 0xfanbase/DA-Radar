{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "audit/event.json",
  "title": "Audit event",
  "description": "One generic, extensible audit-log entry (link-rot checks, staleness checks, ledger-vs-regulator-page coverage checks, verifier pass-rate trend, etc.). Original field design -- the spec names 'audit/*.json' without defining fields (see IMPROVEMENT_BACKLOG.md). Not populated with real data until the audit loop (a later phase) exists.",
  "type": "object",
  "properties": {
    "schema_version": { "const": 1 },
    "event_type": { "type": "string", "minLength": 1 },
    "timestamp": { "type": "string", "format": "date-time" },
    "actor": { "type": "string", "minLength": 1 },
    "summary": { "type": "string", "minLength": 1 },
    "details": { "type": "object" },
    "related_ids": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    }
  },
  "required": [
    "schema_version",
    "event_type",
    "timestamp",
    "actor",
    "summary",
    "details",
    "related_ids"
  ],
  "additionalProperties": false
}
