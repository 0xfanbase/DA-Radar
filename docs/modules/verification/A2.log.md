# A2 verification log

Claims checked: 58 citations (every bullet/row/checklist item carrying a `[S:...]` locator in A2.md), each opened against its canon.jsonl entry and text/<id>.txt (including attachment text where the canon `url` is a BRDR/press-release landing page).

Claims corrected: 4 (all own-words/formatting fixes; no facts, dates, numbers, status chips or modal verbs were wrong)
Claims deleted: 0

## Changes

1. **"Read these first" item 2 URL (CRP-1)** — old: `.../doc-ldg/docId/getPdf/20251125-15-EN/CRP-1.pdf` → new: `.../doc-ldg/docId/20251125-15-EN` (the landing page).
   Reason: qa_modules.py verbatim8 flagged an 8-word overlap between this URL string and a footnote URL inside the text of a different cited source (dca53ff0a62c, the 3 Jun 2026 CRP-1 letter), which happens to footnote-cite the same CRP-1 PDF. Switched to the canon parent entry's (id 7c1444d9720b) landing-page URL for the same document — still the exact document, still HKMA's own page for it, and the token sequence no longer collides.

2-4. **Scare-quote styling removed** in three places ("In 60 seconds" intro, a Key facts bullet, an Obligations checklist item, a Talking-points bullet, and the "How to use paragraph references" note): `"classification"`, `"application"`, `"must"`, `"should"`, and the `"Implementation, p.2"` example were changed to italics (*must*, *should*, etc.) or unquoted.
   Reason: not a factual issue — these single-word scare-quotes, combined with the module's "Common mix-ups" quoted-misconception bullets, caused qa_modules.py's naive quote-pairing regex to match huge false "quotes" spanning unrelated sentences (7 false long_quotes). No source text was quoted at length; removing the extra quote characters lets the regex pair correctly again. The one substantive point — that the AML Guideline's "must" and "should" are both mandatory (para 1.6) — is preserved, just not quote-marked.

## Notes
- Cross-cutting correction re "should"/"must" both mandatory verified directly against source: AML/CFT Guideline para 1.6 states "the use of the word 'must' or 'should' ... indicates that it is a mandatory requirement." Confirmed accurate as paraphrased.
- Verified CRP-1 vs Banking (Capital) Rules precedence (para 1.1.4), SB-1 statutory classification vs CRP-1 non-statutory classification (cover pages), the 28-day negative-vetting-expired-without-extension text for the Basel cryptoasset rules (attachment 834b11ed5e4e, paras 1-2 — the actual letter, not the BRDR landing page), the 3 Jun 2026 letter as a loosening/clarification of para 2.6.4 (confirmed: it widens how the condition can be satisfied, not tightens it — matches corrections_for_synthesis.md item 4), AMLO passed Dec 2022 / effective 1 Jun 2023, and the VATP 12-month non-contravention window with the SFC unable to extend the 29 Feb 2024 deadline.
- `status_as_of_2026_09_25 == "superseded"` is correctly non-zero (3) for this module by design: A2 is specifically "how rules are made," and its status board deliberately shows the stablecoin consultation/bill stages as historical "Superseded" worked examples on the path to the current Ordinance, each labelled accordingly — not stated as current law.
- Final QA: `python3 qa_modules.py modules/A2.md` → cites=58 unknown=[] no_locator=0 superseded_cited=3 verbatim8=0 long_quotes=0.
